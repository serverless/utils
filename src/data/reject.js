import remove from './remove'

/**
 * Alias of [`remove`](#remove).
 *
 * @function
 * @since v0.0.16
 * @category data
 * @param {Function} predicate
 * @param {*} filterable
 * @returns {*}
 */
const reject = remove

export default reject
